import React from "react";import { useLanguage } from "../i18n";export function LanguageToggle() {  const { lang, setLang } = useLanguage();  return (    <div className="flex items-center gap-2 text-sm font-medium">      <button        type="button"        onClick={() => setLang("es")}        className={`transition-opacity ${lang === "es" ? "opacity-100" : "opacity-60"}`}        aria-label="Español"      >        ES      </button>      <span className="opacity-40">|</span>      <button        type="button"        onClick={() => setLang("en")}        className={`transition-opacity ${lang === "en" ? "opacity-100" : "opacity-60"}`}        aria-label="English"      >        EN      </button>    </div>  );}